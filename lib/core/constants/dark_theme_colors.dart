import 'package:flutter/material.dart';

/// Dark theme colors for the application
class DarkThemeColors {
  // Dark backgrounds
  static const Color backgroundPrimary = Color(0xFF0F172A);
  static const Color backgroundSecondary = Color(0xFF1E293B);
  static const Color backgroundTertiary = Color(0xFF334155);

  // Dark surfaces
  static const Color surfacePrimary = Color(0xFF1E293B);
  static const Color surfaceSecondary = Color(0xFF334155);

  // Dark text colors
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFFCBD5E1);
  static const Color textMuted = Color(0xFF64748B);

  // Dark borders
  static const Color borderLight = Color(0xFF334155);
  static const Color borderMedium = Color(0xFF475569);

  // Dark shadows
  static const Color shadowLight = Color(0x1A000000);
  static const Color shadowMedium = Color(0x26000000);
  static const Color shadowHeavy = Color(0x40000000);
}
